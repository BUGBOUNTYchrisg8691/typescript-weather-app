import React, { ReactElement } from 'react'

import DailyCard from './DailyCard';

interface Props {
  
}

export default function Daily({}: Props): ReactElement {
  return (
    <div>
      Daily
    </div>
  )
}

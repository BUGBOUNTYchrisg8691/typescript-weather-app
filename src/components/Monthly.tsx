import React, { ReactElement } from 'react'

import DailyCard from './DailyCard';

interface Props {
  
}

export default function Monthly({}: Props): ReactElement {
  return (
    <div>
      Monthly
    </div>
  )
}

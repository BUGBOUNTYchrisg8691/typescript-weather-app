import React, { ReactElement } from 'react'

import DailyCard from './DailyCard';

interface Props {
  
}

export default function TenDay({}: Props): ReactElement {
  return (
    <div>
      TenDay
    </div>
  )
}

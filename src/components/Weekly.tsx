import React, { ReactElement } from 'react'

import DailyCard from './DailyCard';

interface Props {
  
}

export default function Weekly({}: Props): ReactElement {
  return (
    <div>
      Weekly
    </div>
  )
}
